/**
* oiajsdoijasd
* @author my name, your name
*/
public class Myclass extends A implements B{

    int a;//fuehrt zu Fehler

    Constructor(String f, String d){
        iasjdoi = 2;
        s + 4;
    }

    protected class MyClass implements X {
        int getx();
    }

    /**
    * oiajsd
    * noch mehr Doku
    * @param sind halt da
    */
    @Override
    public static void myMethod(String a, int b) {
        int b;//fuehrt zu Fehler
        {
            int aoisdjoijasd;
            int a += 123;
        }
        int c;//fuehrt zu Fehler

        c = b;
        c += 2;
    }
    public static void myMethod(String a, int b) {
            int d;//fuehrt zu Fehler
            int e;//fuehrt zu Fehler
    }
}



COMMENT
    : '//'+ ~[\n\r]* NEWLINE ->skip
    ;

BLOCK_COMMENT
    : '/*' .*? '*/' ->skip
    ;
